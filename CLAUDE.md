# 貨運行營運系統

跨專案的分工、進度與規則以 `docs/briefs/00-總控.md` 為正本；那五份
`docs/briefs/` 文件異動時，規則先改那裡，這份 `CLAUDE.md` 再視需要同步。

## 回答方式

- 指令先給，解釋放最後或不放。一次講一件事。
- 需要使用者手動操作時，明確講做什麼、在哪裡做、大概多久。
- 全程繁體中文。不新增使用者沒要求的東西。
- 使用者講不出抽象需求時，給看得到的參照物，不要用形容詞去問。
- 使用者描述的使用情境不要當成規格——那是情境，不是規格。
- 不要把問題丟回給使用者，自己判斷、自己建議；只有需要授權、付費、
  本人親自操作的事才問使用者。
- 講完一件事，要把替代方案、退路、壞掉了怎麼收拾一起講完，不要分次補。
- 涉及「某個工具能不能做某件事」的判斷，先查證再講，不要憑印象給笨方法。
- 每一輪都要承接前面所有的討論，不要回答完一題就像重新認識使用者，
  使用者不需要每次重講背景。
- 使用者不是執行者。需要修改的東西由執行者（Claude Code）交出完整版本，
  不要丟片段叫使用者自己拼。

## 通知規則

執行長任務時，遇到下列情況先呼叫 `scripts/notify.ps1` 再停下等待：

- 需要使用者手動執行 SQL
- 需要使用者授權、登入、貼上金鑰
- 遇到無法自行排除的錯誤
- 整包任務全部完成（level 用 done）
- 單一階段超過 10 分鐘（level 用 info，說明進度）

Title 要具體寫「我需要你做什麼」，不要只寫「需要協助」。

## 文件維護規則

每次任務結束前，更新 `docs/工具與帳號清單.md`：新用到的工具、新踩的坑、設定方式的變化都要補進去。

## 工作方式

一次只做一件事，不順手改別的。改動前先確認是否影響 RLS 與權限。
不新增使用者沒要求的功能、制度、名詞。
需要使用者操作時先呼叫 notify.ps1 再停下。

### 額度與中斷處理

遇到 API 用量限制時，等待重置後自動恢復，不視為任務失敗，
恢復後照常完成剩餘階段並回報。

### 問題分級處理

執行任務時如果發現任務範圍外的問題，依嚴重度分流：
- 阻塞現有功能：立刻停下並呼叫 notify.ps1 回報，等待指示
- 體驗瑕疵（不影響使用但不理想）：記錄進 docs/known-issues.md，
  繼續原任務，不要當場處理
- 不影響使用：記錄進 docs/known-issues.md，繼續原任務

### 未驗證資訊的彙整

任何文件裡標記「未驗證」或「需要使用者補充」的項目，
一律同步登記進 docs/pending-info.md。
下次接到新任務包前，先讀這份清單，
若新任務包涵蓋相關領域就一併詢問，不要分次打斷使用者。

### 使用者手冊同步

新增任何使用者需要操作的功能時，
同步檢查 docs/使用者操作手冊.md 是否需要補充對應的操作說明。

## 絕對不要做

把金鑰寫進任何會進 git 的檔案。
沒有備份就執行 DELETE 或 DROP。
把 repo 改成 private（會關掉 Pages）。
直接改線上資料庫的資料來修 bug。
一次改超過三個模組。

## 慣例

介面文字繁體中文。金額用 Roboto Mono。車號一律黃牌樣式。
日期 YYYY-MM-DD。新模組必須同時加：schema、RLS、權限鍵、
選單、CSV 匯出、匯入對應、資料字典。
