# 設定速查卡

一頁到底，只列數值和「去哪拿」，細節說明看 `docs/env.md`／`docs/config-snapshot.md`。

- **Supabase Project URL**：`https://ekzqrpgrxicyvaqagdjp.supabase.co`（這個不是機密，可以直接寫）
- **Publishable（anon）key 去哪拿**：Supabase 後台 → 選 `freight-ops` 專案 → Project Settings → API → 找 `anon` `public` 那把 → 複製
- **Service role（secret）key 去哪拿、放哪**：
  - 去哪拿：Supabase 後台 → 同上路徑 → 找 `service_role` `secret` 那把 → 複製
  - 放在哪個檔案：`.env`（repo 根目錄），格式：
    ```
    SUPABASE_URL=https://ekzqrpgrxicyvaqagdjp.supabase.co
    SUPABASE_SERVICE_KEY=<貼在這裡>
    ```
- **GitHub repo 網址**：<https://github.com/tienan8952-source/freight-ops>
- **GitHub Pages（線上系統）網址**：<https://tienan8952-source.github.io/freight-ops/>
- **`.env` 檔案完整路徑**：`C:\Users\User\Documents\freight-ops\.env`
- **這個檔案存在哪裡最安全**：只留在你自己這台電腦的 `.env`，不要複製貼到聊天訊息、雲端筆記、Email 裡——它已經被 `.gitignore` 排除不會進 git，但只要離開這台電腦、離開這個檔案，就等於外流。
