# TP-07：資料保全與可復原性

## 目錄
- 階段一：主版本文件
- 階段二：備份自動化
- 階段三：設定備份（不含金鑰值）
- 階段四：復原手冊
- 階段五：復原演練
- 階段六：定期檢查
- 階段七：驗證與回報

## 環境
- repo：`C:\Users\User\Documents\freight-ops`
- 線上：`https://tienan8952-source.github.io/freight-ops/`
- Supabase：`https://ekzqrpgrxicyvaqagdjp.supabase.co`

## 執行原則
連續執行，不要中途問使用者要不要繼續。只有需要使用者執行 SQL 或貼金鑰才停下，停前先呼叫 `scripts/notify.ps1`。每階段 commit，全部完成後 push。全繁體中文。若 `docs/project-brief.md` 或 `docs/建置歷程.md` 已存在則跳過對應部分。執行前先讀 `docs/pending-info.md`，這包會回答裡面跟 Supabase／GitHub 相關的項目，讀到的答案要更新回 `pending-info.md` 的狀態。發現任務範圍外的問題依 `CLAUDE.md` 的問題分級規則處理，不要當場打斷任務。

## 階段一｜主版本文件

`docs/project-brief.md`（若不存在才建）：專案背景，內容與 Claude 專案指示一致，標註「這份是主版本」。

`docs/建置歷程.md`（若不存在才建）：記錄 2026-09-16 從零到上線的完整過程：做了什麼、順序為何、遇到什麼問題、怎麼解決。從 git log、docs/、agent_alerts 推導。

## 階段二｜備份自動化

1. `scripts/backup.ps1`：讀 `.env`，用 service_role key 匯出所有資料表為 JSON，存到 `backups/YYYY-MM-DD/`，每張表一個檔。同時產生 `backups/YYYY-MM-DD/manifest.json`：各表筆數、Storage 檔案數、匯出時間、schema 版本。自動保留最近 12 份，更舊的刪除。`backups/` 加進 `.gitignore`（資料不進 git）。
2. `scripts/backup-storage.ps1`：下載 Storage 裡所有檔案到 `backups/storage/`。大檔案分批，可中斷續跑。
3. 前端「系統維護」頁加：顯示最後備份時間與距今天數；超過 7 天在總覽頁顯示紅色提醒；「下載完整備份」按鈕（JSON + CSV zip）。

## 階段三｜設定備份（不含金鑰值）

`docs/config-snapshot.md`，記錄所有可重建的設定：
- Supabase：專案 ID、區域、方案、已啟用功能、Auth 設定（Site URL、Redirect URLs、Email 驗證開關）、Storage bucket 名稱與政策、所有 RLS 政策清單
- GitHub：repo 名稱、可見性、Pages 設定（分支、目錄）
- 本機：需要的執行原則、PATH、Node 版本

同時產生 `supabase/full-schema.sql`：從目前資料庫狀態匯出完整 schema（表、欄位、索引、RLS 政策、觸發器、function），可在空專案一次重建。

這階段順便處理 `pending-info.md` 裡的兩題：Supabase 實際方案、區域、用量、是否有自動備份，以及 GitHub repo 是否要用 PR 流程、是否開 Dependabot 或 secret scanning——把查得到的事實寫進 `config-snapshot.md`，需要使用者決定的維持在 `pending-info.md` 待回答，不要自己替使用者決定。

## 階段四｜復原手冊

`docs/recovery.md`，四種情境各一套完整步驟，每步附可直接複製的指令：

1. **電腦壞了／換新電腦**：從 git clone 到能繼續開發的完整流程
2. **Supabase 專案沒了／被暫停**：建新專案 → 跑 `full-schema.sql` → 還原 JSON 備份 → 還原 Storage → 改前端的 URL 與 key → 驗證
3. **資料被誤刪**：從最近備份還原單一資料表、不影響其他表的做法
4. **Claude 帳號換了**：重貼專案指示（從 `project-brief.md`）、重跑 claude 授權、確認 `CLAUDE.md` 與 commands 仍在

每套最後要有「怎麼確認真的復原成功」的驗證步驟。

## 階段五｜復原演練

實際做一次，證明手冊可行：
1. 跑 `backup.ps1` 產生一份備份
2. 在 Supabase 建一張測試表，塞幾筆資料
3. 刪掉它
4. 照 `docs/recovery.md` 的步驟從備份還原
5. 驗證資料一致
6. 把演練結果寫進 `docs/recovery.md` 的「已驗證」欄位，註明演練日期與發現的問題
7. 清掉測試表

沒驗證過的手冊等於沒有，這階段不能跳過。

## 階段六｜定期檢查

`.claude/commands/健康檢查.md` 擴充為：Supabase 連線狀態、各表筆數（與上次檢查比較）、資料庫容量、Storage 容量、距離免費額度上限多少、最後備份時間、最後一次 Supabase 活動時間（七天無活動會被暫停）、未處理的 agent_alerts、git 未提交的變更、各文件的「最後更新」日期（超過 30 天未更新的列出來）。

`docs/maintenance.md` — 維護節奏：每週該做什麼、每月該做什麼、每季該做什麼，寫成可勾選的清單。

## 階段七｜驗證與回報

1. 跑一次 `backup.ps1` 確認產出正確
2. 跑一次 `/健康檢查` 確認輸出完整
3. commit、push、確認線上 HTTP 200
4. `notify.ps1` 回報完成（level=done）
5. 回報：建了哪些檔案、演練發現什麼問題、`pending-info.md` 裡哪幾項已回答／哪幾項還待使用者決定、有哪些風險是這包沒有涵蓋的、依 `CLAUDE.md` 規則記錄的任何體驗瑕疵或不影響使用的問題

## 執行結果摘要

已於 2026-09-16 全部完成（commit `dcd087f`～`ba84972`）。階段一因 `docs/project-brief.md`、`docs/建置歷程.md` 已存在而跳過。階段五的「建一張測試表」改用既有 `customers` 表的資料列演練（理由與細節見 [docs/recovery.md](../recovery.md)「已驗證」一節），全程沒有需要使用者手動執行 SQL 或貼金鑰的卡點，因此沒有呼叫 `notify.ps1` 中途停下。

演練過程中意外發現並修正了一個比原本認知更根本的問題：`docs/known-issues.md` 原本記錄的「notify.ps1 中文亂碼」其實不是 Supabase 顯示問題，而是 Windows PowerShell 5.1 的 `Invoke-RestMethod` 傳字串 `-Body` 時編碼壞掉，已修正並實測確認、狀態改為「已解決」。詳見 [docs/tools/powershell.md](../tools/powershell.md) 與 [docs/recovery.md](../recovery.md)。

`docs/pending-info.md` 三題：gh token 到期時間已回答（無固定到期日）；GitHub 現況已查清楚（secret scanning 已開、Dependabot 未開、無分支保護），但是否要改仍待使用者決定；Supabase 方案/區域/用量/官方自動備份仍待使用者到後台確認或提供 Management API token，維持待回答（已有本機 `backup.ps1` 自動化作為替代方案）。
